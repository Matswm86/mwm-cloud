package no.mwmai.mwmcloud

import android.app.Application

/**
 * Application entry point. Object graph is constructed by hand — the app is small
 * enough that a DI framework would cost more than it saves.
 */
class MwmCloudApp : Application()
